#guestbook-form.love-quote
  %div
    - if @guestbook.errors.any?
      #error_explanation
        %h2
          = pluralize(@guestbook.errors.count, "error")
          prohibited this entry from being saved:
        %ul
          - @guestbook.errors.full_messages.each do |msg|
            %li= msg
    = form_for(@guestbook, html: {role: 'form' }) do |f|
      .field.control-label.form-group
        = f.label :name
        = f.text_field :name, required: '', class: 'form-control'
      .field.control-label.form-group
        = f.label :message
        %small Please leave a message for the happy couple
        = f.text_area :message, required: '', class: 'form-control', rows: '6'
      .actions.form-group
        = f.submit 'Leave Message', class: "btn btn-default disabled"
