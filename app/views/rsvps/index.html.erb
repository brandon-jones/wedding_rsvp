<h1 class="text-center body-title">Showing all RSVPs</h1>

<div class="white-background">
<table class="table table-striped table-bordered table-hover">
  <thead>
    <tr>
      <th><%= link_to('Name',rsvps_path(:order_by => 'name')) %></th>
      <th><%= link_to('Contact',rsvps_path(:order_by => 'contact')) %></th>
      <th><%= link_to('Attending',rsvps_path(:order_by => 'attending')) %></th>
      <th><%= link_to('Name',rsvps_path(:order_by => 'party_size')) %></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @rsvps.each do |rsvp| %>
      <tr>
        <td><p><%= rsvp.name %></p></td>
        <td><p><%= rsvp.contact %></p></td>
        <td><p><%= rsvp.attending %></p></td>
        <td><p><%= rsvp.party_size %></p></td>
        <td><p><%= link_to 'Edit', edit_rsvp_path(rsvp) %></p></td>
        <td><p><%= link_to 'Destroy', rsvp, method: :delete, data: { confirm: 'Are you sure?' } %></p></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>